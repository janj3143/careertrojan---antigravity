/**
 * SECURITY: Supabase credentials removed.
 * 
 * This embedded app requires architectural review.
 * Credentials should be loaded from environment variables via the backend,
 * not hardcoded in frontend code.
 * 
 * TODO: Either integrate this app into the main mentor portal
 * or remove it entirely and use the canonical backend API.
 */

export const projectId = ''  // REMOVED - configure via backend
export const publicAnonKey = ''  // REMOVED - configure via backend