version: '3.8'
name: careertrojan-antigravity

services:
  reverse-proxy:
    image: nginx:alpine
    container_name: careertrojan-antigravity
    ports:
      - "8600:80"
      - "8643:443"
    volumes:
      - ../nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ../../apps/user-portal/dist:/usr/share/nginx/html/user:ro
      - ../../apps/admin-portal/dist:/usr/share/nginx/html/admin:ro
      - ../../apps/mentor-portal/dist:/usr/share/nginx/html/mentor:ro
    depends_on:
      - shared-backend-api
      - portal-bridge
    networks:
      - careertrojan-net

  portal-bridge:
    container_name: careertrojan-antigravity-bridge
    build:
      context: ../../
      dockerfile: infra/docker/Dockerfile.bridge
    environment:
      - CAREERTROJAN_DATA_ROOT=/data
      - CAREERTROJAN_WORKING_ROOT=/data/working_copy
      - ALLOW_MOCK_DATA=false
      - SERVICE_NAME=portal-bridge
    volumes:
      - ${CAREERTROJAN_WORKING_ROOT}:/data/working_copy:rw
    networks:
      - careertrojan-net
    ports:
      - "8601:8001"


  shared-backend-api:
    container_name: careertrojan-antigravity-backend
    build:
      context: ../../
      dockerfile: infra/docker/Dockerfile.api
    environment:
      - CAREERTROJAN_DATA_ROOT=/data
      - CAREERTROJAN_WORKING_ROOT=/data/working_copy
      - ALLOW_MOCK_DATA=false
      - ALLOW_FALLBACKS=false
      - DATABASE_URL=postgresql://user:pass@postgres:5432/careertrojan
      - REDIS_URL=redis://redis:6379/0
    volumes:
      - ${CAREERTROJAN_DATA_ROOT}:/data:ro
      - ${CAREERTROJAN_WORKING_ROOT}:/data/working_copy:rw
    depends_on:
      - postgres
      - redis
    networks:
      - careertrojan-net
    deploy:
      resources:
        limits:
          memory: 1G
    ports:
      - "8602:8000"

  parser-worker:
    container_name: careertrojan-antigravity-parser
    build:
      context: ../../
      dockerfile: infra/docker/Dockerfile.worker
    environment:
      - SERVICE_NAME=parser-worker
      - CAREERTROJAN_WORKING_ROOT=/data/working_copy
    volumes:
      - ${CAREERTROJAN_WORKING_ROOT}:/data/working_copy:rw
    depends_on:
      - redis
    networks:
      - careertrojan-net
    deploy:
      resources:
        limits:
          memory: 2G

  enrichment-worker:
    container_name: careertrojan-antigravity-enrichment
    build:
      context: ../../
      dockerfile: infra/docker/Dockerfile.worker
    environment:
      - SERVICE_NAME=enrichment-worker
      - CAREERTROJAN_DATA_ROOT=/data
      - CAREERTROJAN_WORKING_ROOT=/data/working_copy
    volumes:
      - ${CAREERTROJAN_DATA_ROOT}:/data:ro
      - ${CAREERTROJAN_WORKING_ROOT}:/data/working_copy:rw
    depends_on:
      - redis
    networks:
      - careertrojan-net
    deploy:
      resources:
        limits:
          memory: 4G

  postgres:
    image: postgres:15-alpine
    environment:
      - POSTGRES_USER=user
      - POSTGRES_PASSWORD=pass
      - POSTGRES_DB=careertrojan
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - careertrojan-net
    ports:
      - "8605:5432"

  redis:
    image: redis:7-alpine
    networks:
      - careertrojan-net
    ports:
      - "8604:6379"

networks:
  careertrojan-net:
    driver: bridge

volumes:
  postgres_data:
