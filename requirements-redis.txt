# Redis Requirements for CareerTrojan Platform
# Install with: pip install -r requirements-redis.txt

# Core Redis client
redis==5.0.1            # Python Redis client
hiredis==2.3.2          # C parser for Redis protocol (performance boost)

# Async Redis support (optional)
aioredis==2.0.1         # Async Redis client

# Redis utilities
redis-py-cluster==2.1.3 # Redis cluster support (optional)
redis-lock==4.0.0       # Distributed locking (optional)

# Serialization (for complex objects)
msgpack==1.0.7          # Fast serialization (optional)

# Type stubs for development
types-redis==4.6.0.20240106
